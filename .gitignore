# secrets — never commit
.env
CLAUDE.local.md
*.token

# local state
*.db
*.db-shm
*.db-wal
data/
.venv/
venv/

# python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# editors / os
.vscode/
.idea/
.DS_Store
.vercel

# stray screenshots pasted into the repo root. Images that belong to the
# project live in assets/ and are committed from there.
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp

# kept locally, deliberately not published: agent instructions, the original
# build plan, and the per-phase design docs the project was built from
AGENTS.md
CLAUDE.md
BUILD_PLAN.md
README-header.md
docs/
